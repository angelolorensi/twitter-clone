import { SlideTweetBoxDirective } from './slide-tweet-box.directive';

describe('SlideTweetBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideTweetBoxDirective();
    expect(directive).toBeTruthy();
  });
});
